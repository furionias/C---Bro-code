#include <stdio.h>

int main(){

    //foreach loop = loop that eases the traversal over an iterable data type

    char names[] = {'A', 'B', 'C', 'D'};
    for(std::string name : names){
        std::cout << i;
    }
    return 0;

}