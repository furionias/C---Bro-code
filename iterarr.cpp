#include <stdio.h>

int main(){

    std::string names[] = {"Asriel", "w", "w"};
    char names[] = {"A", "B", "C", "D"}
    for (int i =0; i<sizeof(students); i++;){
        std::cout << students[i] << "/n";
    }
    return 0;
}